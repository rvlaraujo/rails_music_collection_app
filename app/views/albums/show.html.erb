<p id="notice"><%= notice %></p>
<div class="container">
  <h1>
    <%= @album.name %>
  </h1>
</div>
<div class="col-md">
  <p>
    <strong>Artist:</strong>
    <%= @artist.name %>
  </p>
  <p>
    <strong>Year:</strong>
    <%= @album.year %>
  </p>
</div>
<div class="container">
  <%= link_to 'Edit', edit_album_path(@album) %> |
  <%= link_to 'Back', albums_path %>
</div>
