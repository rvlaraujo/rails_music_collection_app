<p id="notice"><%= notice %></p>
<div class="container">
  <h1>
    <%= @artist.name %>
  </h1>
</div>
<div class="col-md">
  <p>
    <strong>Twitter:</strong>
    <%= @artist.twitter %>
  </p>
</div>
<div class="row">
  <h1>Albums</h1>
</div>
<!-- Artist Albums List -->
<%= render "shared/albums_table" %>
<div class="container">
  <%= link_to 'Back', artists_path, class: 'bg-transparent fw-bold' %>
</div>
